# Project-shared namespace (script contract §4). Deliberately empty of plate
# dimensions: the spec lives on the registered drawing (references/
# plate-drawing.png), and seeding the numbers here would hand over what the
# task exists to measure - reading them off the sheet.
PARAMS = {}
