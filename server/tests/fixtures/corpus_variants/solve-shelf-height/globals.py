# Second independent solution: the proposal applied by editing the DECLARATION
# rather than by persisting an override. Same delivered geometry, a different
# authoring act - which is the point of a second solution (VALIDATION.md §1).
PARAMS = {
    "shelf_z": Param(40.0, min=0.0, max=60.0),
}

plate_t = 6.0
shelf_w = 50.0
post_w = 12.0
